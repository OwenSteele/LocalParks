@using LocalParks.Models.Reports
@model ReportsViewModel
@{
    ViewData["Title"] = "Reports";
}
<div class="bg-white p-4">
    
    <div class="reports-jumbotron-img">
        <div class="p-1 pr-3 pl-3 bg-light rounded w-auto d-inline-block">
            <h1>Local Parks Authority Statistics and Reports</h1>
        </div>
    </div>

    <div class="card row border p-2 bg-light col-10 offset-1">
        <p class="font-weight-bolder m-1">As a taxpayer-funded government institution, we feel it is very important to be transparent about how our parks are run.</p>
        <p class="m-1">We make our data and statistics as accessible as possible, for you to be able to understand how we try to give you the best parks we can.</p>
    </div>
    <div class="m-2">
        <h2>Check out the other report pages, to see graph and visual data representations!</h2>
        <div class="reports-section">
            <h4>Our Parks</h4>
            <div>
                <strong>Parks managed by Parks Authority</strong> @Model.ParkCount.
            </div>
            <div>
                <strong>The total area of these parks</strong> @Model.TotalParkArea m&sup2;.
            </div>
            <div>
                <strong>Average Clubs per park</strong> @Model.AverageClubsPerPark.
            </div>
            <div>
                <strong>Clubs per park ranges from </strong> @Model.MinClubsPerPark to @Model.MaxClubsPerPark.
            </div>
            <div>
                <strong>Average Events currently scheduled per park</strong> @Model.AverageEventsPerPark.
            </div>
            <div>
                <strong>Number of scheduled events per park ranges from </strong> @Model.MinEventsPerPark to @Model.MaxEventsPerPark.
            </div>
            <a class="btn btn-info"  asp-action="Parks" asp-controller="Reports">See Park Graphs</a>
        </div>
        <div class="reports-section">
            <h4>Sports Clubs</h4>
            <div>
                <strong>We have</strong> @Model.ClubCount <strong>Clubs in our parks.</strong>.
            </div>
            <div>
                <strong>On average, each of our clubs have</strong> @Model.AverageClubMembers <strong>members</strong>.
            </div>
            <div>
                <strong>Our largest club is</strong> @Model.LargestClub.Item2, <strong>with </strong> @Model.LargestClub.Item1.
            </div>
            <div>
                <strong>The average membership for each club is </strong> £@Model.AverageClubMembership.
            </div>
            <div>
                @Model.MostPopularSport <strong>is our most popular sport, amongst our clubs</strong>
            </div>
            <a class="btn btn-info"  asp-action="SportsClubs" asp-controller="Reports">See Club Graphs</a>
        </div>
        <div class="reports-section">
            <h4>Organised Upcoming Events</h4>
            <div>
                <strong>Right now,</strong> @Model.EventCount <strong>events are scheduled in our parks.</strong>
            </div>
            <div>
                <strong>In</strong> @Model.MostPopularMonth <strong>, we expect the most number of events to happen!</strong>
            </div>
            <div>
                <strong>With the least current booked in</strong> @Model.LeastPopularMonth
            </div>
            <div>
                <strong>The most popular day of the week for an event is</strong> @Model.MostPopularDayOfWeek.
                <strong>Whereas </strong> @Model.LeastPopularDayOfWeek<strong> is the least popular day for an event.</strong>
            </div>
            <a class="btn btn-info"  asp-action="ParkEvents" asp-controller="Reports">See Park Event Graphs</a>
        </div>
        <div class="reports-section">
            <h4>Supervisors and Staff</h4>
            <div>
                <strong>Currently, on average our supervisors have been protecting parks for</strong> @Model.AverageTenure years.
            </div>
            <div>
                <strong>On average a Supervisor earns</strong> £@Model.AverageSalary pa.
            </div>
            <div>
                <strong>The correlation between Salary and area of park that they are repsonsible for is:</strong> R = @Model.RValueSalaryParkArea.
            </div>
            <div>
                <strong>The correlation between Salary and their tenure  is:</strong> R = @Model.RValueSalaryTenure.
            </div>
            <div>
                <strong>The longest serving supervisor has been pretecting our parks for</strong> @Model.LongestTenure years!
            </div>
            <a class="btn btn-info"  asp-action="Supervisors" asp-controller="Reports">See Staff Graphs</a>
        </div>
        <div class="reports-section">
            <h4>Our Shop</h4>
            <div>
                <strong>Since opening, our shop has had</strong> @Model.TotalOrders <strong>orders.</strong>
            </div>
            <div>
                <strong>With</strong> @Model.TotalItemsOrdered <strong>items ordered in total.</strong>
            </div>
            <div>
                <strong>On average each order has </strong> @Model.AverageOrderItems <strong>items.</strong>
            </div>
            <div>
                <strong>Each order we have processed has an overall average value of</strong> £@Model.AverageOrderValue <strong>. Whic goes straight back into supporting our parks!</strong>
            </div>
            <a class="btn btn-info" asp-action="Shop" asp-controller="Reports">See Shop Graphs</a>
        </div>
        <div>
        </div>
    </div>
</div>