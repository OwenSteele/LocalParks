@model ChartModel

<canvas id="reportChart"></canvas>

<script>
    document.addEventListener('DOMContentLoaded', (event) => {

        let ctx = document.getElementById('reportChart');
        let data = @Html.Raw(Model.Json);
        let reportChart = new Chart(ctx, data);
    });
</script>